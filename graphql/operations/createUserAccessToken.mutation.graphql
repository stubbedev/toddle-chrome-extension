mutation createUserAccessToken($input: GenerateUserAccessTokenInput!) {
  platform {
    generateUserAccessToken(input: $input) {
      warning
      response {
        id
        serviceType
        accessToken
      }
    }
  }
}
