mutation bulkCreateStandardRubricDescriptors($input: [CreateStandardRubricDescriptorInput!]) {
  planner {
    bulkCreateStandardRubricDescriptors(input: $input) {
      isSuccess
      warning
      response {
        ...standardRubricDescriptorItem
      }
    }
  }
}
