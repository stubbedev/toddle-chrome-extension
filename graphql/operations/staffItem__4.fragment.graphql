fragment attendanceStatisticCategoryItem on AttendanceStatisticCategoryItem {
  percentage
  item {
    ...categoryItem
  }
}

fragment staffItem on Staff {
  id
  courses(orderBy: TITLE, orderByDirection: ASC, filters: $filters) {
    ...courseItem
  }
}
