query getSubjectGroupDetails($id: ID!) {
  node(id: $id, type: SUBJECT_GROUP) {
    id
    ... on SubjectGroup {
      id
      name
      subjects {
        id
        name
        isArchived
      }
    }
  }
}
