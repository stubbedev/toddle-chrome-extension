mutation internalToolsGenericMutation($apiType: INTERNAL_TOOL_API_TYPE_ENUM!, $params: JSON!) {
  platform {
    internalToolsGenericMutation(input: {apiType: $apiType, params: $params}) {
      isSuccess
      warning
      data
    }
  }
}
