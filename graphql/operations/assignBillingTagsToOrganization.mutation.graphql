mutation assignBillingTagsToOrganization($input: [TagEntityMapInput!]) {
  platform {
    upsertEntityTagsMapping(input: $input) {
      response {
        tagId
        entityId
        entityType
      }
    }
  }
}
