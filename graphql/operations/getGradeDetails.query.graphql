query getGradeDetails($ids: [ID!]) {
  platform {
    grade(ids: $ids) {
      id
      name
      plannerTemplates {
        id
        body
        name
        type
        plannerName
        unitType
      }
    }
  }
}
