query getOrganizationTemplates($id: ID!, $filters: OrganizationTemplateFilter) {
  node(id: $id, type: ORGANIZATION) {
    ... on Organization {
      id
      templates(filters: $filters) {
        id
        body
      }
    }
  }
}
