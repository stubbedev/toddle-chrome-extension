mutation resetCourseInviteCode($courseIds: [NumericId!]!) {
  platform {
    resetClassQRCode(input: {classIds: $courseIds}) {
      isSuccess
      warning
      affectedCount
      activityLogId
    }
  }
}
