mutation removeStandardRubricCriterias($input: [ID!]) {
  planner {
    removeStandardRubricCriterias(input: $input) {
      isSuccess
      warning
      response {
        ...standardRubricCriteriaItem
      }
    }
  }
}
