mutation updateProjectGroupSettings($projectGroupId: ID!, $settings: [ProjectGroupSettingsInput!]) {
  project {
    updateProjectGroupSettings(input: {projectGroupId: $projectGroupId, settingArray: $settings}) {
      isSuccess
      warning
    }
  }
}
