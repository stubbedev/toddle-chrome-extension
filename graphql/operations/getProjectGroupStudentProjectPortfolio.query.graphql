query getProjectGroupStudentProjectPortfolio($id: ID!, $filters: StudentProjectPortfolioFilters) {
  node(id: $id, type: PROJECT_GROUP) {
    id
    ... on ProjectGroup {
      id
      studentProjectPortfolio(filters: $filters) {
        totalCount
        edges {
          node {
            id
          }
        }
      }
    }
  }
}
