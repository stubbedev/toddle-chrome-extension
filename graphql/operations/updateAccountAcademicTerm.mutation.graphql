mutation updateAccountAcademicTerm($input: UpdateAccountAcademicTermInput!) {
  organizationManagement {
    updateAccountAcademicTerm(input: $input) {
      isSuccess
      warning
      academicTerm {
        ...accountGradingPeriodItem
      }
    }
  }
}
