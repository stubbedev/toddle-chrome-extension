mutation upsertLoginOption($input: LoginOptionInput!) {
  organizationManagement {
    upsertLoginOption(input: $input) {
      isSuccess
      data {
        id
        type
        metadata
        service
      }
      message
    }
  }
}
