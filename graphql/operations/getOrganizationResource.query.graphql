query getOrganizationResource($id: ID!, $libraryBannerFilters: LibraryPremiumBannerFilters) {
  node(id: $id, type: ORGANIZATION_RESOURCE) {
    id
    ... on OrganizationResource {
      ...organizationResourceQuestionItem
    }
  }
}
