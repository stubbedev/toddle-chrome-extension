query getExternalAssessmentRecordSetEligibleStudents($filters: OrgConnectionFilterInput) {
  platform {
    organization {
      id
      students(filters: $filters) {
        totalCount
        edges {
          node {
            ...userNameFields
            profileImage
            sourceId
          }
        }
      }
    }
  }
}
