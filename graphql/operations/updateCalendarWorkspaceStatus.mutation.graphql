mutation updateCalendarWorkspaceStatus($input: UpdateCalendarWorkspaceStatusInput!) {
  platform {
    updateCalendarWorkspaceStatus(input: $input) {
      isSuccess
      probe
      calendarWorkspace {
        id
        provider
        domain
        status
      }
    }
  }
}
