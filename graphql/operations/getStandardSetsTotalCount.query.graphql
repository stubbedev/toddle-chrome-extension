query getStandardSetsTotalCount($id: ID!, $filters: PlannerElementNodeSetFilters) {
  node(id: $id, type: CURRICULUM_PROGRAM) {
    id
    ... on CurriculumProgram {
      id
      plannerElementNodeSets(filters: $filters) {
        totalCount
      }
    }
  }
}
