fragment organizationResourceItemCollection on OrganizationResource {
  id
  label
  type
  resourceType
  entity {
    id
    ...subjectItem
  }
  childrenV2(filters: $collectionFilters) {
    ...collectionItem
  }
}
