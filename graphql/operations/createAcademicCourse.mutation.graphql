mutation createAcademicCourse($input: CreateAcademicCourseInput!) {
  planner {
    createAcademicCourse(input: $input) {
      id
      label
      code
      learningCourses {
        totalCount
      }
    }
  }
}
