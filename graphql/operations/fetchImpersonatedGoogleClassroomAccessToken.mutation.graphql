mutation fetchImpersonatedGoogleClassroomAccessToken($input: ImpersonatedGoogleClassroomAccessTokenInput!) {
  planner {
    fetchImpersonatedGoogleClassroomAccessToken(input: $input) {
      isSuccess
      accessToken
      subjectEmail
    }
  }
}
