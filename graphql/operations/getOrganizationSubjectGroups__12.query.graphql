query getOrganizationSubjectGroups($id: ID!, $filters: SubjectGroupFilterInput) {
  node(id: $id, type: ORGANIZATION) {
    ... on Organization {
      id
      subjectGroups(filters: $filters) {
        id
        name
        type
      }
    }
  }
}
