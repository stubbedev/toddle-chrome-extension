query getOrganizationCourses($id: ID!, $filters: CourseFilterInput) {
  node(id: $id, type: ORGANIZATION) {
    id
    ... on Organization {
      courses(filters: $filters) {
        edges {
          node {
            id
            title
            curriculumProgram {
              id
            }
          }
        }
      }
    }
  }
}
