query getStudentPlanModulePermission($orgId: ID!) {
  platform {
    organization(id: $orgId) {
      id
      aclPermissionsListView(filters: {rnpType: STAFF}) {
        id
        permission
      }
    }
  }
}
