query getOrganizationSubjectGroupIds($id: ID!, $filters: SubjectGroupFilterInput) {
  node(id: $id, type: ORGANIZATION) {
    id
    ... on Organization {
      id
      subjectGroups(filters: $filters) {
        id
      }
    }
  }
}
