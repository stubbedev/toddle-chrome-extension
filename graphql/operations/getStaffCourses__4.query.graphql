query getStaffCourses($id: ID!, $filters: CourseFilter) {
  node(id: $id, type: STAFF) {
    ... on Staff {
      id
      coursesV2(filters: $filters) {
        edges {
          node {
            id
            title
          }
        }
      }
    }
  }
}
