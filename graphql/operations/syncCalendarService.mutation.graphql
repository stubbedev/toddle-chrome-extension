mutation syncCalendarService($input: IntegrationSyncCalendarInput!) {
  integration {
    integrationSyncCalendar(input: $input) {
      allow
      message
      job {
        id
        status
      }
    }
  }
}
