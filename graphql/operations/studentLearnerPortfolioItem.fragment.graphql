fragment studentLearnerPortfolioItem on StudentLearnerPortfolioConnection {
  totalCount
  edges {
    node {
      id
      student {
        ...userItem
      }
      postCount
      lastModifiedAt
    }
  }
}
