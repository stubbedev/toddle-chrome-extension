query getEventTypeOptions($organizationId: ID!) {
  platform {
    organization(id: $organizationId) {
      id
      calendarEventTags {
        id
        label
        colorCode
      }
    }
  }
}
