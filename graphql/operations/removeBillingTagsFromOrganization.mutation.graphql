mutation removeBillingTagsFromOrganization($input: [TagEntityMapInput!]) {
  platform {
    deleteEntityTagMapping(input: $input) {
      response {
        tagId
        entityId
        entityType
      }
    }
  }
}
