mutation updateGenericRubricCriterias($input: [UpdateGenericRubricCriteriaInput!]) {
  planner {
    updateGenericRubricCriterias(input: $input) {
      isSuccess
      warning
      response {
        ...genericRubricCriteriaItem
      }
    }
  }
}
