query getXlsxTemplateForTranscript($id: ID!, $input: DownloadExcelTemplateForTranscriptInput!) {
  node(id: $id, type: YEAR_GROUP) {
    id
    ... on YearGroup {
      downloadExcelTemplateForTranscript(input: $input) {
        ...downloadExcelResponseItem
      }
    }
  }
}
