query getLearnerPortfolioCollections($id: ID!, $skipPostData: Boolean!, $collectionFilters: OrganizationResourceFilter, $postFilters: OrganizationResourceFilter) {
  node(id: $id, type: ORGANIZATION_RESOURCE) {
    id
    ... on OrganizationResource {
      ...organizationResourceItemCollection
    }
  }
}
