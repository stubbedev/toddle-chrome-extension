mutation removeGenericRubricCriterias($input: [ID!]) {
  planner {
    removeGenericRubricCriterias(input: $input) {
      isSuccess
      warning
      response {
        ...genericRubricCriteriaItem
      }
    }
  }
}
