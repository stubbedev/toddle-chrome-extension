mutation bulkCreateStandardRubricCriteria($input: [CreateStandardRubricCriteriaInput!]) {
  planner {
    bulkCreateStandardRubricCriteria(input: $input) {
      isSuccess
      warning
      response {
        ...standardRubricCriteriaItem
      }
    }
  }
}
