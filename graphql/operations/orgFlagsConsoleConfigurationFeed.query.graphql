query orgFlagsConsoleConfigurationFeed($filters: GenericConfigurationFeedFilters) {
  platform {
    genericConfigurationFeed(filters: $filters) {
      edges {
        node {
          id
          configuration
          valueType
        }
      }
    }
  }
}
