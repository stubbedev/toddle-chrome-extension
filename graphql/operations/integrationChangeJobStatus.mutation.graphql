mutation integrationChangeJobStatus($id: ID!, $status: INTEGRATION_JOB_STATUS_ENUM!, $subTasks: [IntegrationInputSubTask!]) {
  integration {
    integrationChangeJobStatus(input: {jobId: $id, status: $status, subTasks: $subTasks})
  }
}
