query getOrganizationAcademicYears {
  platform {
    organization {
      academicYears {
        id
        startDate
        endDate
        isCurrentAcademicYear
        isUpcomingAcademicYear
        transitionReviewStatus
      }
    }
  }
}
