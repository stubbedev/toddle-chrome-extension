query allClassesOfOrganisation($id: ID!) {
  node(id: $id, type: ORGANIZATION) {
    id
    ... on Organization {
      courses {
        edges {
          node {
            id
            title
          }
        }
      }
    }
  }
}
