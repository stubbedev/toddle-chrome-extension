query getStaffGrades($id: ID!, $filterByWorksheets: Boolean!) {
  node(id: $id, type: STAFF) {
    id
    ... on Staff {
      id
      allGrades {
        unitPlanCount
        unitPlans @include(if: $filterByWorksheets) {
          id
          resourceLibrary(filters: {groupType: WORKSHEET}) {
            edges {
              node {
                id
              }
            }
          }
        }
        curriculumProgram {
          ...curriculumProgramBasicDetailsItem
        }
        ...gradeItem
      }
    }
  }
}

fragment worksheetItem on FlowSectionItem {
  id
  title {
    id
    value
  }
  createdBy {
    id
    profileImage
    firstName
    lastName
  }
  fields(uids: ["subjectLevel"]) {
    id
    value
    resolvedMinimalTree {
      ... on ResolvedFieldGenericTags {
        id
        type
        genericTags {
          id
          label
        }
      }
    }
  }
}
