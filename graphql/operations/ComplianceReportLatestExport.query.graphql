query ComplianceReportLatestExport($id: ID!) {
  complianceReport {
    complianceReport(id: $id) {
      id
      exports {
        id
        format
        status
        downloadUrl
        exportedAt
        createdAt
      }
    }
  }
}
