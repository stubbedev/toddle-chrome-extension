mutation unarchiveSchoolGroup($input: SchoolGroupIdInput!) {
  platform {
    unarchiveSchoolGroup(input: $input) {
      isSuccess
      warning
      schoolGroup {
        id
        isArchived
      }
    }
  }
}
