mutation batchUpdateBehaviourLocations($input: BatchUpdateBehaviourLocationsInput!) {
  platform {
    batchUpdateBehaviourLocations(input: $input) {
      isSuccess
      errors {
        entityId
        code
        message
        incidentCount
      }
    }
  }
}
