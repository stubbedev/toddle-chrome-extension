mutation archiveSchoolGroup($input: SchoolGroupIdInput!) {
  platform {
    archiveSchoolGroup(input: $input) {
      isSuccess
      warning
      schoolGroup {
        id
        isArchived
      }
    }
  }
}
