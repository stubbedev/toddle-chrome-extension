query getTrainings($filters: AcademyFilters) {
  platform {
    academy(filters: $filters) {
      foundations {
        id
        trainings {
          id
          name
          workrampTrainingId
        }
      }
    }
  }
}
