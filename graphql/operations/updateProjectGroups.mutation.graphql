mutation updateProjectGroups($input: [ProjectGroupUpdateInput!]!) {
  documentation {
    updateProjectGroups(input: $input) {
      isSuccess
      projectGroups {
        id
        isEnabled
      }
    }
  }
}
