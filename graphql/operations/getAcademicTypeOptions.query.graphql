query getAcademicTypeOptions($organizationId: ID!) {
  platform {
    organization(id: $organizationId) {
      id
      curriculumPrograms {
        id
        academicProgram {
          id
          type
        }
      }
    }
  }
}
