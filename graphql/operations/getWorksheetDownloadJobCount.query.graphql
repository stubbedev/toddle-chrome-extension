query getWorksheetDownloadJobCount($id: ID!, $filters: OrganizationIntegrationJobFilterInput) {
  node(id: $id, type: ORGANIZATION) {
    id
    ... on Organization {
      id
      jobFeed(filters: $filters) {
        totalCount
      }
    }
  }
}
