mutation createTags($input: [GenericTagInsertInput!]!) {
  platform {
    insertGenericTagsV2(input: $input) {
      id
      label
      displaySequence
      icon {
        id
        url
      }
    }
  }
}
