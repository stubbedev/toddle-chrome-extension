query getRootNodesFromSet($id: ID!, $nodesFilters: PlannerElementNodeFilters, $nodeSetFilters: PlannerElementNodeSetsByPlannerElementFilters) {
  node(id: $id, type: PLANNER_ELEMENT_SET) {
    id
    ... on PlannerElementSet {
      plannerElementNodeSets(filters: $nodeSetFilters) {
        id
        plannerElementNodeSetWrapperV2 {
          ... on PlannerElementNodeSetWrapper {
            publishedData(filters: $nodesFilters) {
              edges {
                node {
                  ...plannerElementRootNodeItem
                }
              }
            }
          }
          ... on PlannerBenchmarkSetWrapper {
            publishedData(filters: $nodesFilters) {
              edges {
                node {
                  ...plannerBenchmarkRootItem
                }
              }
            }
          }
          ... on PlannerATLSetWrapper {
            publishedData(filters: $nodesFilters) {
              edges {
                node {
                  ...plannerATLRootItem
                }
              }
            }
          }
          ... on PlannerLearnerProfileSetWrapper {
            publishedData(filters: $nodesFilters) {
              edges {
                node {
                  ...plannerLearnerProfileRootItem
                }
              }
            }
          }
          ... on PlannerConceptSetWrapper {
            publishedData(filters: $nodesFilters) {
              edges {
                node {
                  ...plannerConceptRootItem
                }
              }
            }
          }
        }
      }
    }
  }
}

query getAiConverstion($id: ID!) {
  node(id: $id, type: AI_CONVERSATION) {
    ... on AiConversation {
      id
      messages {
        totalCount
        edges {
          node {
            id
          }
        }
      }
    }
  }
}

query getAiConverstion($id: ID!) {
  node(id: $id, type: AI_CONVERSATION) {
    ... on AiConversation {
      id
      messages {
        totalCount
        edges {
          node {
            id
          }
        }
      }
    }
  }
}

query getAiConverstion($id: ID!) {
  node(id: $id, type: AI_CONVERSATION) {
    ... on AiConversation {
      id
      messages {
        totalCount
        edges {
          node {
            id
          }
        }
      }
    }
  }
}

query getAiConverstion($id: ID!) {
  node(id: $id, type: AI_CONVERSATION) {
    ... on AiConversation {
      id
      messages {
        totalCount
        edges {
          node {
            id
          }
        }
      }
    }
  }
}

query getAiConverstion($id: ID!) {
  node(id: $id, type: AI_CONVERSATION) {
    ... on AiConversation {
      id
      messages {
        totalCount
        edges {
          node {
            id
          }
        }
      }
    }
  }
}
