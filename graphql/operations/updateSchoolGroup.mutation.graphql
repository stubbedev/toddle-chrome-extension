mutation updateSchoolGroup($input: UpdateSchoolGroupInput!) {
  platform {
    updateSchoolGroup(input: $input) {
      isSuccess
      warning
      schoolGroup {
        ...schoolGroupListItem
        ...schoolGroupCounts
      }
    }
  }
}
