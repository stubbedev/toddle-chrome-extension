query getActionMappedCategories($id: ID!) {
  node(id: $id, type: BEHAVIOUR_ACTION) {
    ... on BehaviourAction {
      id
      mappedCategories(input: {first: 100}) {
        edges {
          node {
            id
            name
            color
          }
        }
      }
    }
  }
}
