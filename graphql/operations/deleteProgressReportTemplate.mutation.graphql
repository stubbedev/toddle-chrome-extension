mutation deleteProgressReportTemplate($id: ID!) {
  documentation {
    deleteProgressReportTemplate(input: {id: $id}) {
      isSuccess
      warning
      error {
        status
      }
    }
  }
}
