query GetAdmissionUnits {
  admission {
    admissionUnits {
      id
      name
      curriculumPrograms {
        id
        label
        acronym
        grades {
          id
          name
        }
      }
    }
  }
}
