mutation restoreDeletedAssignment($restoreAssignmentInput: RestoreDeletedAssignmentInput!) {
  platform {
    restoreDeletedAssignment(input: $restoreAssignmentInput) {
      response {
        id
      }
    }
  }
}
