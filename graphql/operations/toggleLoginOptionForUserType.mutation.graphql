mutation toggleLoginOptionForUserType($input: [ToggleLoginOptionForUserTypeInput!]) {
  organizationManagement {
    toggleLoginOptionForUserType(input: $input) {
      isSuccess
      data {
        id
        type
        supportedUserTypes
      }
    }
  }
}
