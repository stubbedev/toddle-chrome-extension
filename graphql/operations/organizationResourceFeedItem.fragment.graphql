fragment organizationResourceFeedItem on Organization {
  id
  organizationResources(types: $types, visibleTo: $visibleTo, searchText: $searchText, filters: $organizationResourceFilter) {
    ...organizationResourceItem
  }
}
