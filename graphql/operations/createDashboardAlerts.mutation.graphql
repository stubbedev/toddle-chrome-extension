mutation createDashboardAlerts($input: CreateDashboardAlertsInput!) {
  platform {
    createDashboardAlerts(input: $input) {
      isSuccess
      warning
      dashboardAlert {
        ...dashboardAlertFragment
      }
    }
  }
}
