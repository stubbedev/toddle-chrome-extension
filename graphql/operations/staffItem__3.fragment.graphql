fragment staffItem on Staff {
  id
  coursesV2(first: $first, after: $after, filters: $filters, orderBy: TITLE, orderByDirection: ASC) {
    totalCount
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        ...courseItem
      }
    }
  }
}
