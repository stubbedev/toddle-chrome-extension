query getOrgTeachersStudentNumberDetails($id: ID!) {
  node(id: $id, type: ORGANIZATION) {
    id
    ... on Organization {
      id
      students {
        totalCount
      }
      staff {
        totalCount
      }
    }
  }
}
