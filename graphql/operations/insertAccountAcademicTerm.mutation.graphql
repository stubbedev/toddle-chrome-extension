mutation insertAccountAcademicTerm($input: InsertAccountAcademicTermInput!) {
  organizationManagement {
    insertAccountAcademicTerm(input: $input) {
      isSuccess
      warning
      academicTerm {
        ...accountGradingPeriodItem
      }
    }
  }
}
