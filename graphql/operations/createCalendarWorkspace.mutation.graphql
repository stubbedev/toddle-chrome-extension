mutation createCalendarWorkspace($input: CreateCalendarWorkspaceInput!) {
  platform {
    createCalendarWorkspace(input: $input) {
      isSuccess
      calendarWorkspace {
        id
        provider
        domain
        status
      }
    }
  }
}
