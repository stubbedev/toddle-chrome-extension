fragment studentPostItem on Student {
  id
  posts(last: 21, after: $after, filters: $filters) {
    totalCount
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        ...postSelectionItem
      }
    }
  }
}
