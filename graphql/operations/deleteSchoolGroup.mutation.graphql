mutation deleteSchoolGroup($input: SchoolGroupIdInput!) {
  platform {
    deleteSchoolGroup(input: $input) {
      isSuccess
      warning
      schoolGroup {
        id
        isArchived
      }
    }
  }
}
