mutation createAIConversation($input: CreateAiConversationInput!) {
  ai {
    createAiConversation(input: $input) {
      isSuccess
      response {
        id
        entityType
        metadata
      }
    }
  }
}
