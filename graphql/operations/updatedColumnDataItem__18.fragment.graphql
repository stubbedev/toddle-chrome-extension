fragment conceptRemovedFeed on PlannerConceptSetWrapper {
  dataSetEditChanges(filters: {locale: $defaultLocale}) {
    removedFeed(first: $first, after: $after) {
      totalCount
      edges {
        node {
          ...publishedBasicDetailsConceptItem
        }
      }
      pageInfo {
        ...pageInfoFields
      }
    }
  }
}

fragment updatedColumnDataItem on UpdatedColumnData {
  gradeScaleId
  columnName
  oldValue
  newValue
}
