mutation updateAcademicCourse($input: UpdateAcademicCourseInput!) {
  planner {
    updateAcademicCourse(input: $input) {
      id
      code
      label
      learningCourses {
        totalCount
      }
    }
  }
}
