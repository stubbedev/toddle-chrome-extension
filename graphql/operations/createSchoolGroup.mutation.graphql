mutation createSchoolGroup($input: CreateSchoolGroupInput!) {
  platform {
    createSchoolGroup(input: $input) {
      isSuccess
      warning
      schoolGroup {
        ...schoolGroupListItem
        ...schoolGroupCounts
      }
    }
  }
}
