mutation updateProjectField($input: BulkUpdateProjectInput!) {
  documentation {
    bulkUpdateProject(input: $input) {
      isSuccess
      warning
      projects {
        id
        fields {
          id
        }
      }
    }
  }
}
