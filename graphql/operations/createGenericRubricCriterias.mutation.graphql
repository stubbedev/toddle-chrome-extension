mutation createGenericRubricCriterias($input: [CreateGenericRubricCriteriaInput!]!) {
  planner {
    createGenericRubricCriterias(input: $input) {
      isSuccess
      warning
      response {
        ...genericRubricCriteriaItem
      }
    }
  }
}
