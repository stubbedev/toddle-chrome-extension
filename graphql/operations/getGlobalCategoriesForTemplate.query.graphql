query getGlobalCategoriesForTemplate($id: ID!) {
  node(id: $id, type: CURRICULUM_PROGRAM) {
    id
    ... on CurriculumProgram {
      id
      globalCategory {
        edges {
          node {
            id
            name
          }
        }
      }
    }
  }
}
