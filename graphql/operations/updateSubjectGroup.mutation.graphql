mutation updateSubjectGroup($id: ID!, $isArchived: Boolean) {
  platform {
    updateSubjectGroup(input: {id: $id, isArchived: $isArchived}) {
      isSuccess
      warning
      subjectGroup {
        id
        name
        isArchived
      }
    }
    __typename
  }
}
