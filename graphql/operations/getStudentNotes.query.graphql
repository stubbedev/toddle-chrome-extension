query getStudentNotes($studentId: ID!, $filters: StudentNoteFilters) {
  node(id: $studentId, type: STUDENT) {
    id
    ... on Student {
      studentNotes(filters: $filters) {
        ...studentNoteItem
      }
    }
  }
}
