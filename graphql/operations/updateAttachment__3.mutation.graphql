mutation updateAttachment($input: AttachmentUpdateInput!) {
  platform {
    updateAttachment(input: $input) {
      ...attachmentItem
      modalityAttachment {
        ... on ModalityFile {
          id
          content
        }
      }
    }
  }
}
