query getActionAppliedSchools($id: ID!) {
  node(id: $id, type: BEHAVIOUR_ACTION) {
    ... on BehaviourAction {
      id
      appliedSchools(input: {first: 300}) {
        edges {
          node {
            id
            label
            type
            acronym
            logo
            attachments {
              id
              url
            }
            academicProgram {
              id
              label
            }
          }
        }
      }
    }
  }
}
