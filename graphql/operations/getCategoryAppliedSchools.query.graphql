query getCategoryAppliedSchools($id: ID!) {
  node(id: $id, type: BEHAVIOUR_CATEGORY) {
    ... on BehaviourCategory {
      id
      appliedSchools(input: {first: 300}) {
        edges {
          node {
            id
            label
            type
            acronym
            logo
            attachments {
              id
              url
            }
            academicProgram {
              id
              label
            }
          }
        }
      }
    }
  }
}
