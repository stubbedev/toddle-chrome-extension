query getOrganizationDetails($id: ID!, $globalConstantsFilter: GlobalConstantsFilterOptions, $curriculumProgramFilters: CurriculumProgramFilters) {
  node(id: $id, type: ORGANIZATION) {
    ... on Organization {
      ...organizationDetailsItem
    }
  }
}
