fragment studentPostItem on Student {
  id
  posts(last: 9, after: $after, filters: $filters) {
    totalCount
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        ...postItem
      }
    }
  }
}
