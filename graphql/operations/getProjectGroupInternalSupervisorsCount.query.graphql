query getProjectGroupInternalSupervisorsCount($projectGroupId: ID!, $supervisorFilters: ProjectGroupSupervisorFilters) {
  node(id: $projectGroupId, type: PROJECT_GROUP) {
    ... on ProjectGroup {
      id
      supervisors(filters: $supervisorFilters) {
        totalCount
      }
    }
  }
}
