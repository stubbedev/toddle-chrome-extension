mutation createOrgLink($input: CreatePlaygroundOrgLinkInput!) {
  playground {
    createOrgLink(input: $input) {
      isSuccess
      warning
      accessLink {
        ...playgroundAccessLinkFragment
      }
    }
  }
}
