query getTemplatesCount($id: ID!) {
  node(id: $id, type: CURRICULUM_PROGRAM) {
    id
    ... on GenericCurriculumProgram {
      externalAssessmentTemplateFeed(input: {filters: {archivalState: ALL}}) {
        totalCount
      }
    }
  }
}
