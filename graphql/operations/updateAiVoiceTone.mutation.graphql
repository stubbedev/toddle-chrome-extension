mutation updateAiVoiceTone($input: [VoiceToneEditInput!]) {
  ai {
    updateAiVoiceTone(input: $input) {
      isSuccess
      response {
        id
        ...progressReportAIVoiceTones
      }
    }
  }
}
