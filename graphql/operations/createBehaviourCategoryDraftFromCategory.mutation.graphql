mutation createBehaviourCategoryDraftFromCategory($input: BehaviourCategoryDraftFromCategoryInput!) {
  platform {
    createBehaviourCategoryDraftFromCategory(input: $input) {
      id
      rootNodes
      nodes
    }
  }
}
