query getTemplateVersionChanges($input: ReportTemplateVersionChangesInput!) {
  documentation {
    internalToolsData {
      progressReport {
        templateVersionChanges(input: $input) {
          section
          path
          field
          action
          oldValue
          newValue
        }
      }
    }
  }
}
