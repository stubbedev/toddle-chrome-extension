mutation editInsightDataSource($input: EditInsightDataSourceInput!) {
  platform {
    editInsightDataSource(input: $input) {
      uploadStatus
      resourceId
      successData
      errorCount
      errorData
    }
  }
}
