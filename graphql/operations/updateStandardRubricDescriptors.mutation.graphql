mutation updateStandardRubricDescriptors($input: [UpdateStandardRubricDescriptorInput!]) {
  planner {
    updateStandardRubricDescriptors(input: $input) {
      isSuccess
      warning
      response {
        ...standardRubricDescriptorItem
      }
    }
  }
}
