mutation applyBehaviourConfigToSchools($input: ApplyBehaviourConfigToSchoolsInput!) {
  platform {
    applyBehaviourConfigToSchools(input: $input) {
      isSuccess
      errors {
        entityId
        code
        message
        incidentCount
      }
    }
  }
}
