fragment plannerElementNodeCountItem on PlannerElementNode {
  id
  standardCountWrapper {
    assignment {
      totalCount
    }
    portfolio {
      totalCount
    }
    unitPlan {
      totalCount
    }
  }
}
