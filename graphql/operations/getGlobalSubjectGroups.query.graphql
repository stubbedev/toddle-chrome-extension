query getGlobalSubjectGroups($subjectGroupFilters: GlobalSubjectGroupFilters) {
  globalConstants {
    globalSubjectGroups(filters: $subjectGroupFilters) {
      id
      name
      sourceSubjects {
        id
        name
      }
    }
  }
}
