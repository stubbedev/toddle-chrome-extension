query getConfigurationValues($filters: configurationValueFilters!) {
  platform {
    getConfigurationValues(filters: $filters) {
      values {
        value
        id
        entityGroup {
          entities {
            entityId
            entityType
          }
        }
        regionId
      }
    }
  }
}
