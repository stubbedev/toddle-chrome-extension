query getProjectGroupSupervisorsCount($projectGroupId: ID!, $academicYearIds: [ID!]) {
  node(id: $projectGroupId, type: PROJECT_GROUP) {
    ... on ProjectGroup {
      id
      supervisors(filters: {academicYearIds: $academicYearIds}) {
        totalCount
      }
    }
  }
}
