mutation prepareDraftForAcademicYearTransitionStates($input: PrepareDraftForAcademicYearTransitionStatesInput!) {
  planner {
    prepareDraftForAcademicYearTransitionStates(input: $input) {
      isSuccess
      warning
    }
  }
}
