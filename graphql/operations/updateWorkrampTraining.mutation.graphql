mutation updateWorkrampTraining($input: UpdateWorkrampTrainingInput!) {
  platform {
    updateWorkrampTraining(input: $input) {
      training {
        id
        name
      }
      isSuccess
      warning
    }
  }
}
