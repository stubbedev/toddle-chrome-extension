mutation updateStandardRubricCriterias($input: [UpdateStandardRubricCriteriaInput!]) {
  planner {
    updateStandardRubricCriterias(input: $input) {
      isSuccess
      warning
      response {
        ...standardRubricCriteriaItem
      }
    }
  }
}
