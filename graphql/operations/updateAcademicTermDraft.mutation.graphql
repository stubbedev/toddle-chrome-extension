mutation updateAcademicTermDraft($id: ID!, $draftData: AcademicTermDraftUpdateInput!) {
  planner {
    updateAcademicTermDraft(id: $id, draftData: $draftData) {
      key
      label
      startDate
      endDate
    }
  }
}
