mutation bulkCreateRubricIndicators($input: [RubricIndicatorInput!]) {
  planner {
    bulkCreateRubricIndicators(input: $input) {
      isSuccessful
      warnings
      response {
        indicator {
          ...indicatorItem
        }
        descriptors {
          ...descriptorItem
        }
      }
    }
  }
}
