mutation createBehaviourCategoryDraftNodes($input: BehaviourCategoryDraftNodesCreateInput!) {
  platform {
    createBehaviourCategoryDraftNodes(input: $input) {
      id
      rootNodes
      nodes
    }
  }
}
