query getOrganizationConstants($organizationId: ID!, $filter: GlobalConstantsFilterOptions) {
  node(id: $organizationId, type: ORGANIZATION) {
    ... on Organization {
      id
      globalConstants(filter: $filter) {
        id
        name
        value
      }
    }
  }
}
