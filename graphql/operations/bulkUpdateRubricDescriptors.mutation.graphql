mutation bulkUpdateRubricDescriptors($input: [RubricDescriptorUpdateInput!]) {
  planner {
    bulkUpdateRubricDescriptors(input: $input) {
      isSuccessful
      warnings
      response {
        ...descriptorItem
      }
    }
  }
}
