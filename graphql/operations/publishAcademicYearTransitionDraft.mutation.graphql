mutation publishAcademicYearTransitionDraft($input: PublishAcademicYearTransitionDraftInput!) {
  planner {
    publishAcademicYearTransitionDraft(input: $input) {
      isSuccess
      workflowId
      message
      activityLogId
    }
  }
}
