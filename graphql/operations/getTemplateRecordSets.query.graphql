query getTemplateRecordSets($id: ID!) {
  node(id: $id, type: EXTERNAL_ASSESSMENT_TEMPLATE) {
    id
    ... on ExternalAssessmentTemplate {
      associatedRecordSets {
        edges {
          id
          name
        }
      }
    }
  }
}
