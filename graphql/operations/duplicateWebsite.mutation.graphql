mutation duplicateWebsite($input: DuplicateCurriculumWebsiteInput!) {
  planner {
    duplicateCurriculumWebsite(input: $input) {
      isSuccess
      curriculumWebsite {
        id
        status
      }
    }
  }
}
