query getCourseUserMap($id: ID!, $filters: CourseUserMapFilter, $userArchivedFilters: UserArchivedFilters) {
  node(id: $id, type: COURSE) {
    id
    ... on Course {
      ...courseItem
    }
  }
}
