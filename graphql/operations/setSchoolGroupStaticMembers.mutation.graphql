mutation setSchoolGroupStaticMembers($input: SetSchoolGroupStaticMembersInput!) {
  platform {
    setSchoolGroupStaticMembers(input: $input) {
      isSuccess
      warning
      schoolGroup {
        ...schoolGroupListItem
        ...schoolGroupCounts
      }
    }
  }
}
