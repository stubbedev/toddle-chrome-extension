mutation logoutStudentWithSignInCodeResetMutation($id: ID!, $updateStudentSignInCode: Boolean) {
  platform {
    updateUser(input: {id: $id, logoutUser: true, updateStudentSignInCode: $updateStudentSignInCode}) {
      ... on Student {
        signInCode
      }
    }
  }
}
