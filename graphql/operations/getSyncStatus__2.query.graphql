query getSyncStatus($id: ID!, $type: [JOB_TYPE_ENUM!]) {
  node(id: $id, type: ORGANIZATION) {
    id
    ... on Organization {
      syncStatus(type: $type) {
        id
        status
        curriculumProgramId
        acronym
        jobType
      }
    }
  }
}
