mutation createDuplicateRoutine($input: CreateDuplicateRoutineInput!) {
  platform {
    createDuplicateRoutine(input: $input) {
      warning
      isSuccess
      routine {
        id
        label
      }
    }
  }
}
