query getIncomingTransferIds($cpId: ID!, $type: ENTITY_TYPE_ENUM!, $input: StudentTransferRequestsInput!) {
  node(id: $cpId, type: $type) {
    ... on CurriculumProgram {
      id
      incomingStudentTransferRequestsFeed(input: $input) {
        edges {
          node {
            id
          }
        }
      }
    }
  }
}
