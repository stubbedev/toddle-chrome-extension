mutation removeUserIdentityFromMultiAccessIdentity($input: removeUserIdentityFromMultiAccessIdentityInput!) {
  platform {
    removeUserIdentityFromMultiAccessIdentity(input: $input) {
      isSuccess
    }
  }
}
