mutation createTemplateFieldHelpText($input: TemplateFieldHelpTextInput) {
  planner {
    createTemplateFieldHelpText(input: $input) {
      id
      uid
      type
      helpText {
        id
      }
    }
  }
}
