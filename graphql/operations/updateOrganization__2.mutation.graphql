mutation updateOrganization($id: ID!, $startDate: String!, $endDate: String!) {
  organizationManagement {
    manageOrganizationAcademicYear(input: {id: $id, newAcademicYear: {startDate: $startDate, endDate: $endDate}}) {
      warning
      response {
        ...academicYear
      }
    }
  }
}
