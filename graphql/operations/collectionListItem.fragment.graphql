fragment collectionListItem on OrganizationResourceConnection {
  edges {
    node {
      id
      label
      type
      resourceType
      entity {
        id
        ...subjectItem
      }
    }
  }
}
