fragment postItem on Post {
  id
  comments {
    ... on CommentConnection {
      totalCount
      unreadCount
      messageEdges {
        node {
          ...messageItem
        }
      }
    }
  }
}
