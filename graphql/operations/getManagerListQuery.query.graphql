query getManagerListQuery($filters: MultiAccessIdentityFeedFilters) {
  platform {
    multiAccessIdentityFeed(filters: $filters) {
      pageInfo {
        hasNextPage
        endCursor
      }
      edges {
        node {
          id
          identityId
          firstName
          lastName
        }
      }
    }
  }
}
