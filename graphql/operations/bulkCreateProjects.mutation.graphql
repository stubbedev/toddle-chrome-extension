mutation bulkCreateProjects($input: CreateProjectInput!) {
  documentation {
    bulkCreateProjects(input: $input) {
      projects {
        id
        students {
          id
        }
      }
    }
  }
}
