mutation createAITutorConversation($input: CreateAiConversationInput!) {
  ai {
    createAiConversation(input: $input) {
      isSuccess
      response {
        id
        entityType
        entitySubType
        metadata
      }
    }
  }
}
