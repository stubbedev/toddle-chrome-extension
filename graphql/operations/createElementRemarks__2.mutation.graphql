mutation createElementRemarks($input: [IBPYPElementRemarkInput!]) {
  planner {
    createElementRemarks(input: $input) {
      response {
        id
        createdBy {
          ...staffItem
        }
        createdAt
      }
    }
  }
}
