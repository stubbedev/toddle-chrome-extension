query getOrganizationSubjectGroups($subjectGroupFilters: SubjectGroupFilterInput) {
  platform {
    organization {
      subjectGroups(filters: $subjectGroupFilters) {
        id
        name
        curriculumProgram {
          id
        }
      }
    }
  }
}
