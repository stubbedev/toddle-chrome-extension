mutation removeStandardRubricDescriptors($input: [ID!]) {
  planner {
    removeStandardRubricDescriptors(input: $input) {
      isSuccess
      warning
      response {
        ...standardRubricDescriptorItem
      }
    }
  }
}
