query getBenchmarkStandardSets($globalSubjects: [ID!], $searchText: String) {
  globalConstants {
    benchmarkStandardSets(globalSubjects: $globalSubjects, searchText: $searchText) {
      ...standardSetItem
    }
  }
}
