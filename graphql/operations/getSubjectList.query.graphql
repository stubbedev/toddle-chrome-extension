query getSubjectList($organizationId: ID!, $SubjectFilters: SubjectFilterInput) {
  node(id: $organizationId, type: ORGANIZATION) {
    ... on Organization {
      id
      subjects(filters: $SubjectFilters) {
        id
        name
      }
    }
  }
}
