fragment courseProgressReportItem on Course {
  id
  courseProgressReportTasks(filters: {searchText: $searchText, academicTermIds: $academicTermIds}) {
    totalCount
    edges {
      node {
        ...progressReportItem
      }
    }
  }
}
