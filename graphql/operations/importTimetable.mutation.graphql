mutation importTimetable($input: ImportTimetableInput!) {
  integration {
    importTimetable(input: $input) {
      uploadStatus
      errorData
      successData {
        added
      }
    }
  }
}
