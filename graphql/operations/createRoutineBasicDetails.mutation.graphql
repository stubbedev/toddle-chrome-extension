mutation createRoutineBasicDetails($input: CreateRoutineBasicDetailsInput!) {
  platform {
    createRoutineBasicDetails(input: $input) {
      isSuccess
      warning
      routine {
        id
        rotationDays {
          id
          label
        }
        routineMode
      }
    }
  }
}
