mutation uploadFamilyMembersDetailTemplate($input: EntityBulkUploadInput!) {
  platform {
    entityBulkUpload(input: $input) {
      errorData
      successData {
        added
        updated
        deleted
        reInvited
        mapped
      }
      uploadStatus
      activityLogId
    }
  }
}
