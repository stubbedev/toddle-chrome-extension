mutation deleteLike($userId: ID!, $parentId: ID!, $parentType: PARENT_TYPE_ENUM!) {
  documentation {
    deleteLike(input: {userId: $userId, parentId: $parentId, parentType: $parentType}) {
      totalCount
      edges {
        node {
          ...likeItem
        }
      }
    }
  }
}
