mutation manageBehaviourSeverities($input: ManageBehaviourSeveritiesInput!) {
  platform {
    manageBehaviourSeverities(input: $input) {
      isSuccess
      warning
      severities {
        id
        name
        level
        color
        associatedIncidentsCount
      }
    }
  }
}
