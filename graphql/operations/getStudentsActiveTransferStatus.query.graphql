query getStudentsActiveTransferStatus($ids: [ID!]) {
  multiNode(ids: $ids, type: STUDENT) {
    id
    ... on Student {
      ...userNameFields
      profileImage
      email
      sourceId
      hasAnActiveTransfer
    }
  }
}
