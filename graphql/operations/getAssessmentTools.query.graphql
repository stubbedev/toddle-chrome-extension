query getAssessmentTools($id: ID!) {
  node(id: $id, type: PROJECT_GROUP) {
    ... on ProjectGroup {
      id
      assessmentTools {
        id
        ... on MypObjectiveRubric {
          criteria {
            id
            highestMark
          }
        }
      }
    }
  }
}
