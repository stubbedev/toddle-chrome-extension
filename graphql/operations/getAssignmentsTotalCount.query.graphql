query getAssignmentsTotalCount($courseId: ID!, $filters: AssignmentFeedFilters) {
  node(id: $courseId, type: COURSE) {
    id
    ... on Course {
      id
      assignments(filters: $filters) {
        totalCount
      }
      assignments {
        totalCount
      }
    }
  }
}
