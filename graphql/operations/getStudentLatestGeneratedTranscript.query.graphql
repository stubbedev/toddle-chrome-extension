query getStudentLatestGeneratedTranscript($id: ID!, $studentTranscriptFilter: StudentTranscriptFilter) {
  node(id: $id, type: STUDENT) {
    id
    ... on Student {
      ...studentLatestGeneratedTranscriptItem
    }
  }
}
