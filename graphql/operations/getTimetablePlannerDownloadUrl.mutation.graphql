mutation getTimetablePlannerDownloadUrl($input: TimetablePlannerDownloadUrlInput!) {
  platform {
    timetablePlannerDownloadUrl(input: $input) {
      url
      isSuccess
      warning
      longRunningTaskCreated
    }
  }
}
