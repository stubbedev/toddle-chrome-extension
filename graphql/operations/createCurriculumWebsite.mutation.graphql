mutation createCurriculumWebsite($input: CreateCurriculumWebsiteInput!) {
  planner {
    createCurriculumWebsite(input: $input) {
      isSuccess
      curriculumWebsite {
        id
      }
    }
  }
}
