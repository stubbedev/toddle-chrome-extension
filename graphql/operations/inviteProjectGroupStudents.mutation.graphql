mutation inviteProjectGroupStudents($projectGroupId: ID!, $addArray: [ID!], $removeArray: [ID!]) {
  documentation {
    inviteProjectGroupStudents(input: {projectGroupId: $projectGroupId, student: {added: $addArray, removed: $removeArray}})
  }
}
