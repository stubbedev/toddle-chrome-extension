fragment questionSubmissionConnectionItem on QuestionSubmissionConnection {
  totalCount
  totalQuestionCount
  totalQuestionAttempted
  edges {
    node {
      ...questionSubmissionItem
    }
  }
}
