query getCompleteOrganizationStudents($filters: OrgConnectionFilterInput) {
  platform {
    organization {
      id
      students(filters: $filters) {
        edges {
          node {
            id
          }
        }
      }
    }
  }
}
