query GetSignedUrl($input: SignedUrlsInput!) {
  content {
    getSignedUrl(input: $input) {
      signedUrls {
        originalUrl
        signedUrl
      }
      isSuccess
      warning
    }
  }
}
