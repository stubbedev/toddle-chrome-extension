mutation updateConfigurablePermission($id: ID!, $permissions: [ConfigurablePermissionToggleInput!]!) {
  platform {
    toggleOrganizationConfigurablePermission(input: {organizationId: $id, permissions: $permissions})
  }
}
