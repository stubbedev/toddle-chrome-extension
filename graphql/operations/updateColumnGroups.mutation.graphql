mutation updateColumnGroups($input: [UpdateExternalAssessmentColumnGroupInput!]!) {
  externalAssessment {
    updateExternalAssessmentColumnGroups(input: $input) {
      isSuccess
      columnGroups {
        id
        name
      }
    }
  }
}
