query getAiConverstions($filters: AiConversationFilters) {
  ai {
    aiConversations(filters: $filters) {
      edges {
        node {
          id
          entityType
          metadata
        }
      }
    }
  }
}
