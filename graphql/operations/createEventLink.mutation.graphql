mutation createEventLink($input: CreatePlaygroundEventLinkInput!) {
  playground {
    createEventLink(input: $input) {
      isSuccess
      warning
      accessLink {
        ...playgroundAccessLinkFragment
      }
    }
  }
}
