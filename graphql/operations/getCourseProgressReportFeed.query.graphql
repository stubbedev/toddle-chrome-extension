query getCourseProgressReportFeed($courseId: ID!, $searchText: String!, $academicTermIds: [ID!]) {
  node(id: $courseId, type: COURSE) {
    id
    ... on Course {
      ...courseProgressReportItem
    }
  }
}
