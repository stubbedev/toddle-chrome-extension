query getActivityLike($id: ID!) {
  node(id: $id, type: STUDENT_ACTIVITY) {
    id
    ... on StudentActivity {
      id
      student {
        ...userItem
      }
      likes {
        totalCount
        edges {
          node {
            id
            user {
              id
              ...userItem
              ... on FamilyMember {
                children {
                  ...userItem
                }
              }
            }
          }
        }
      }
    }
  }
}
